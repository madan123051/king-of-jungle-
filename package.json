{
  "name": "candyboom",
  "version": "1.0.0",
  "description": "Match 3 Puzzle Game with Firebase Backend",
  "main": "index.html",
  "scripts": {
    "dev": "live-server",
    "build": "mkdir -p public && cp index.html public/ && cp game.html public/ && cp firebase-config.json public/ && cp -r styles public/ 2>/dev/null || true && cp -r assets public/ 2>/dev/null || true",
    "deploy:firebase": "firebase deploy",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:firestore": "firebase deploy --only firestore:rules",
    "serve": "firebase serve"
  },
  "keywords": [
    "game",
    "match3",
    "puzzle",
    "firebase"
  ],
  "author": "Candyboom Team",
  "license": "MIT",
  "dependencies": {
    "firebase": "^10.7.0"
  },
  "devDependencies": {
    "firebase-tools": "^13.0.0",
    "live-server": "^1.2.2"
  }
}
